[pytest]
testpaths = tests backend/tests
markers =
    slow: marks tests that require container tooling
    acl: access control checks
    integration: integration-level tests
    smoke: basic smoke tests
    worker: worker/scheduler tests
